Crea una función que reciba dos listas de números del mismo tamaño y devuelva una nueva lista con la suma de los elementos uno a uno.
function sumarListas (lista1,lista2) {
    let listaSumas = [];
    if (lista1.length == 0 || lista2.length ==0) {
      console.log('Una o ambas listas no contienen ningun elemento.');
      return;
    }
    if (lista1.length != lista2.length) {
      console.log('Las listas no son del mismo tamano, ingrese el mismo numero de elementos para realizar la suma');
      return;
    }
    for (let j=0;j<lista1.length;j++) {
      listaSumas.push (lista1[j] + lista2[j]);
    }
    return listaSumas;
}
listalfa = [9, 2, 30, 12];
listbeta = [-34, -1, 2, -14];
sumas = sumarListas (listalfa,listbeta);
  
Crea una función que reciba una lista de números y devuelva una nueva lista con el cuadrado de cada número.
function listaCuadrados (listaOG) {
    let listaFinal = [];
    if (listaOG.length == 0) {
      console.log('La lista no contiene ningun elemento.');
      return listaFinal;
    }
    for (let k=0;k<listaOG.length;k++) {
      listaFinal.push (listaOG[k] * listaOG[k]);
    }
    return listaFinal;
}
listaRaiz = [2, 3, 4, 5];
cuadrados = listaCuadrados (listaRaiz);
